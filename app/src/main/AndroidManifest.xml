<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Animation">
        <activity
            android:name=".cardslider.cardSlider"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".circlemenu.circlemenu"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".cardslider.DetailsActivity"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".FoldingCell.Foldingcell"></activity>
        <activity android:name=".spacetablayoutdemo.spaceTabLayout" />
        <activity android:name=".ShimmerLayout" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".textWriter" />
    </application>

</manifest>